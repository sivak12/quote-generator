<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <title>Quote Generator</title>
  </head>
  <body>
    <div class="container">
      <!-- <h2>An Inspiring Quote.</h2> -->

      <div class="card mt-5">
        <div class="card-body">
          <h4 class="card-title" id="quote_area">"<%= quote.fullQuote %>"</h4>
          <div class="card-text mb-2" id="author">- <%= quote.author %></div>
        </div>
      </div>

      <a href="/" class="btn btn-primary mt-4"> Get another inspiring quote </a>
    </div>

    <!--- <script>
      //var quotes_list = "% quotes_list  %>";

      const quotes_list = "";

      function getQuote() {
        const rand = Math.floor(Math.random() * quotes_list.length);
        const quote = quotes_list[rand];

        // find a random quote
        //const numQuotes = await Quote.estimatedDocumentCount();
        //const rand = Math.floor(Math.random() * numQuotes);
        //const quote = await Quote.findOne().skip(rand);

        console.log("quote:" + quote);
        const quoteArea = document.getElementById("quote_area");
        const authorArea = document.getElementById("author");
        quoteArea.innerHTML = `"${quote.fullQuote}"`;
        authorArea.innerHTML = `- ${quote.author}`;
      }

      window.onload = getQuote;
    </script> -->
  </body>
</html>
